package com.bpm.fsr.process;

import java.util.HashMap;
import java.util.Map;

import org.junit.Test;
import org.kie.api.runtime.process.ProcessInstance;

public class CantTrainPortStatusUnitTest extends FSRProcessJunitBaseTestCase{/*

	@Test
	public  void testCantTrainPortStatus() {
    	Map<String, Object> params = getInputParams();

    	ProcessInstance processInstance = ksession.startProcess("CantTrainPortStatus",params);
		ksession.fireAllRules();
        
        assertNotNull(processInstance);
        
        testInstructCustomertoRemoveDSL(processInstance);
        assertProcessInstanceCompleted(processInstance.getId(), ksession);
	}
	
	private static Map<String, Object> getInputParams() {
    	Map<String, Object> params = new HashMap<String, Object>();
    	params.put("agentName", "krisv");

    	return params;
	}
	
	public static void testInstructCustomertoRemoveDSL(ProcessInstance processInstance){
		completeDisplayNextBestAction(processInstance, "krisv", "Instruct customer to remove DSL Cable from Modem and check if the DSL light is on", "Instruct customer to remove DSL Cable from Modem and check light is on",getInputParams());
	}
	
*/}
